// Seket Admin configuration (copy to config.js and fill in)
// Leave values empty to run in localStorage-only mode.
// Optional SEKET_TEAM can be any short label to scope rows for your squad.
window.SEKET_SUPABASE_URL = "";
window.SEKET_SUPABASE_ANON_KEY = "";
window.SEKET_TEAM = ""; // e.g. "U12" or "A-lag"

